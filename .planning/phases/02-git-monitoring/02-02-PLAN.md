---
phase: 02-git-monitoring
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [internal/git/repo.go, internal/git/monitor.go, internal/activity/activity.go]
autonomous: true
---

<objective>
Add staged changes detection to git monitor, completing the git activity tracking suite.

Purpose: Staged changes indicate work-in-progress and should earn credit even before committing. This completes the git monitoring requirement from PROJECT.md: "Monitor Git activity (commits, staged changes, branch switches)".
Output: Git monitor detects and emits activity events when files are staged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-git-monitoring/02-01-SUMMARY.md

# Source files from 02-01
@internal/git/repo.go
@internal/git/monitor.go
@internal/activity/activity.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add staged file detection to Repo</name>
  <files>internal/git/repo.go</files>
  <action>
    1. Add method GetStagedFiles() ([]string, error) to Repo:
       - Run `git diff --cached --name-only` to get list of staged files
       - Return slice of file paths
       - Handle empty result (no staged files) gracefully
    2. Add method GetStagedFileCount() (int, error):
       - Convenience wrapper returning len(GetStagedFiles())
    3. Add method HasStagedChanges() (bool, error):
       - Returns true if GetStagedFileCount() > 0
  </action>
  <verify>go build ./internal/git/... && go vet ./internal/git/...</verify>
  <done>Repo can detect and list staged files</done>
</task>

<task type="auto">
  <name>Task 2: Emit activity on staged changes</name>
  <files>internal/git/monitor.go, internal/activity/activity.go</files>
  <action>
    1. Add GitStage ActivityType to internal/activity/activity.go
    2. Add lastStagedCount int field to Monitor struct
    3. In Monitor polling loop, also check HasStagedChanges():
       - Track lastStagedCount
       - If staged count increased: emit GitStage activity with file count and file names (up to 5)
       - Only emit when count INCREASES (not on every poll)
       - Include details: "files_staged": count, "file_list": comma-separated names
    4. Reset lastStagedCount to 0 when a commit is detected (staged files became commit)
  </action>
  <verify>go build ./internal/... && go vet ./internal/...</verify>
  <done>Monitor emits GitStage activity when files are staged</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/daemonflow` succeeds
- [ ] `go vet ./...` reports no issues
- [ ] Staging a file triggers GitStage activity
- [ ] Staging multiple files shows correct count
- [ ] Committing resets staged tracking (no spurious events)
- [ ] `daemonflow activities` shows GitStage events
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Staged changes detected within poll interval
- Activity details include file count and names
- No duplicate events for same staged files
</success_criteria>

<output>
After completion, create `.planning/phases/02-git-monitoring/02-02-SUMMARY.md`
</output>
