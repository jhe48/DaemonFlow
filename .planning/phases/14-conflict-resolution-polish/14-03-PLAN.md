---
phase: 14-conflict-resolution-polish
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified: []
autonomous: false
---

<objective>
End-to-end verification that pet evolution and task sync work correctly in a real usage scenario.

Purpose: Confirm the bug fixes from plans 01 and 02 resolve the issues identified in the context document. This is a manual verification checkpoint.

Output: Confirmed working v2.0 system with pet evolution and task sync functioning correctly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-conflict-resolution-polish/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build complete system</name>
  <files></files>
  <action>
Build both daemon and TUI to ensure latest code is compiled:

```bash
cd /home/jackyhe0402/daemonflow
go build ./...
cd tui && cargo build --release
```
  </action>
  <verify>Both builds succeed without errors</verify>
  <done>Fresh builds of daemon and TUI ready for testing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pet evolution fix (level sync) and task sync logging</what-built>
  <how-to-verify>
## Test 1: Pet Level Display

1. Start daemon with a watched project directory
2. Start TUI
3. Observe the pet ASCII art and level display
4. Make some commits or file changes to earn XP
5. Once level increases (check XP progress), verify:
   - Pet title shows new level (e.g., "Pet Lv.2 (Healthy)")
   - ASCII art has evolved (more detailed cat)

## Test 2: Task Sync

1. Ensure TASKS.md exists in watched directory with some tasks:
   ```markdown
   - [ ] Test task one
   - [ ] Test task two
   - [x] Completed task
   ```
2. Start daemon (watch logs for "Initial task sync:" messages)
3. Start TUI
4. Verify tasks appear in the task list panel
5. Add a new task to TASKS.md and save
6. Verify:
   - Daemon logs show "TaskFileChanged:" message
   - New task appears in TUI within a few seconds (or press 'r')

## Test 3: Both Together

1. Complete a task (mark with [x] in TASKS.md)
2. Verify: Task completion awards XP (progress bar moves)
3. Verify: Task list updates to remove completed task
  </how-to-verify>
  <resume-signal>Type "approved" if both pet evolution and task sync work correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Pet level display updates when XP increases
- [ ] Pet ASCII art changes at level thresholds
- [ ] Tasks from TASKS.md appear in TUI
- [ ] Task changes sync within seconds
</verification>

<success_criteria>
- User confirms pet evolution works
- User confirms task sync works
- No critical bugs remaining
</success_criteria>

<output>
After completion, create `.planning/phases/14-conflict-resolution-polish/14-03-SUMMARY.md`
</output>
