---
phase: 19-tui-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tui/src/ipc/protocol.rs
  - tui/src/ipc/client.rs
  - tui/src/app.rs
  - tui/src/ui.rs
autonomous: true
---

<objective>
Add stats dashboard panel to TUI showing productivity at a glance.

Purpose: Surface Phase 18's streak and weekly summary data in the TUI so users can see their productivity trends without leaving the terminal.
Output: TUI with dashboard panel showing current/longest streak, weekly stats (tasks, commits, XP, time), and productive day count.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-streaks-summaries/18-01-SUMMARY.md

# Source files
@internal/ipc/protocol.go
@tui/src/ipc/protocol.rs
@tui/src/ipc/client.rs
@tui/src/app.rs
@tui/src/ui.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Rust protocol types for streak and weekly summary</name>
  <files>tui/src/ipc/protocol.rs</files>
  <action>
Add types mirroring Go's IPC types:
1. Add request type constants:
   - REQUEST_TYPE_GET_PRODUCTIVITY_STREAK = "get_productivity_streak"
   - REQUEST_TYPE_GET_WEEKLY_SUMMARY = "get_weekly_summary"

2. Add response structs (must match Go JSON field names):
   - ProductivityStreakData: current_streak, longest_streak, last_active_date, total_active_days
   - GetProductivityStreakResponse: streak (ProductivityStreakData)
   - DailySummaryData: date, tasks_completed, tasks_created, commits, files_changed, xp_earned, time_earned_mins, time_spent_mins, net_time_mins, is_productive
   - WeeklySummaryData: week_start, week_end, total_tasks_completed, total_commits, total_xp_earned, total_time_earned_mins, total_time_spent_mins, productive_days, daily_breakdown (Vec<DailySummaryData>)
   - GetWeeklySummaryResponse: summary (WeeklySummaryData)

Use serde with #[serde(default)] for optional fields. Match Go JSON field naming exactly (snake_case).
  </action>
  <verify>cargo build --package tui compiles without errors</verify>
  <done>All protocol types added and compile successfully</done>
</task>

<task type="auto">
  <name>Task 2: Add IPC client methods for streak and weekly summary</name>
  <files>tui/src/ipc/client.rs</files>
  <action>
Add two client methods following existing patterns (e.g., get_tasks):

1. get_productivity_streak(&self) -> Result<ProductivityStreakData>:
   - Send REQUEST_TYPE_GET_PRODUCTIVITY_STREAK request (no payload needed)
   - Parse response data as GetProductivityStreakResponse
   - Return the inner .streak field

2. get_weekly_summary(&self) -> Result<WeeklySummaryData>:
   - Send REQUEST_TYPE_GET_WEEKLY_SUMMARY request (no payload needed - defaults to current week)
   - Parse response data as GetWeeklySummaryResponse
   - Return the inner .summary field

Both methods should follow the existing error handling pattern (bail! on failure, context for missing data).
  </action>
  <verify>cargo build --package tui compiles; verify methods exist with correct signatures</verify>
  <done>Both IPC client methods implemented and compiling</done>
</task>

<task type="auto">
  <name>Task 3: Add dashboard state to App and fetch on update</name>
  <files>tui/src/app.rs</files>
  <action>
1. Add app state fields:
   - dashboard_streak: Option<ProductivityStreakData>
   - dashboard_weekly: Option<WeeklySummaryData>
   - show_dashboard: bool (toggles between task list and dashboard view)

2. Initialize fields in App::new():
   - dashboard_streak: None
   - dashboard_weekly: None
   - show_dashboard: false

3. Add update_dashboard(&mut self) method:
   - If daemon_connected, call ipc_client.get_productivity_streak() and store in dashboard_streak (ignore errors)
   - If daemon_connected, call ipc_client.get_weekly_summary() and store in dashboard_weekly (ignore errors)
   - This is non-critical data - don't set last_error on failure

4. Call update_dashboard() inside update_state() after updating tasks

5. Add keybinding in InputMode::Normal:
   - 'd' key toggles show_dashboard between true/false

Import required types from protocol.rs.
  </action>
  <verify>cargo build --package tui compiles; TUI runs and 'd' key responds</verify>
  <done>Dashboard state fields added, update method fetches data, 'd' toggles show_dashboard</done>
</task>

<task type="auto">
  <name>Task 4: Add dashboard panel to UI</name>
  <files>tui/src/ui.rs</files>
  <action>
Replace the task list with a dashboard panel when app.show_dashboard is true:

1. In render(), check app.show_dashboard:
   - If true: call render_dashboard(app, frame, chunks[3]) instead of render_tasks
   - If false: existing render_tasks behavior

2. Create render_dashboard(app: &App, frame: &mut Frame, area: Rect):
   - Title: " Dashboard (d: tasks) "
   - Layout: use multi-line Paragraph with Line/Span for formatting

   Content structure (fit in ~8 lines available):
   ```
   Week: Jan 27 - Feb 02
   Streak: 5 days (Best: 12)

   This Week:
     Tasks: 23 | Commits: 45 | XP: 1,250
     Time: +2h 15m earned | 1h 30m spent
     Productive: 4/7 days
   ```

   Formatting:
   - If dashboard_weekly is None, show "Loading..." or "No data"
   - Use Color::Cyan for labels, Color::White for values
   - Format time as hours:mins (e.g., 135 mins → "2h 15m")
   - Productive days: X/7 format
   - Show streak from dashboard_streak if available, fallback to app.current_streak

3. Update controls line when in dashboard view:
   - Show "d: tasks" hint to return to task list
   - Update the existing controls string to reflect current view

Keep the layout simple - this is a glance-able summary, not a detailed report.
  </action>
  <verify>cargo build --package tui compiles; run TUI and press 'd' to see dashboard</verify>
  <done>Dashboard panel renders with streak, weekly stats, and can toggle back to tasks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo build --package tui succeeds
- [ ] TUI launches and connects to daemon
- [ ] Press 'd' shows dashboard panel with streak and weekly stats
- [ ] Press 'd' again returns to task list
- [ ] Dashboard shows meaningful data (tasks completed, commits, time earned/spent)
- [ ] No panics or errors during normal operation
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dashboard displays productivity stats at a glance
- Users can toggle between task list and dashboard with 'd' key
</success_criteria>

<output>
After completion, create `.planning/phases/19-tui-dashboard/19-01-SUMMARY.md`
</output>
