---
phase: 08-graveyard
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified: [internal/graveyard/streak.go, internal/graveyard/graveyard.go, internal/ipc/protocol.go, internal/ipc/server.go, tui/src/app.rs, tui/src/ui.rs, tui/src/ipc.rs]
autonomous: true
---

<objective>
Implement streak tracking and display in TUI.

Purpose: Track consecutive days without pet death as a motivational metric. Streak resets on death, persists across daemon restarts, displays in TUI.
Output: Streak calculation, IPC exposure, TUI display of current streak.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan summaries for graveyard context
@.planning/phases/08-graveyard/08-01-SUMMARY.md
@.planning/phases/08-graveyard/08-02-SUMMARY.md

# Graveyard package with death/resurrection records
@internal/graveyard/graveyard.go

# IPC protocol for adding streak to state response
@internal/ipc/protocol.go

# TUI app and UI for displaying streak
@tui/src/app.rs
@tui/src/ui.rs
@tui/src/ipc.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add streak calculation to graveyard</name>
  <files>internal/graveyard/streak.go, internal/graveyard/graveyard.go</files>
  <action>
Create streak tracking in graveyard package:

streak.go:
- StreakInfo struct: CurrentStreak (int - days), LongestStreak (int - days), LastDeathDate (time.Time or nil)
- Function CalculateStreak(deaths []DeathRecord) StreakInfo:
  - If no deaths, streak = days since daemon first run (or epoch if no records)
  - If deaths exist, streak = days since last death
  - Track longest streak by checking gaps between deaths
  - Use time.Now().Truncate(24*time.Hour) for day calculations

graveyard.go:
- Add method GetStreakInfo() StreakInfo - calls CalculateStreak with current records
- Streak resets to 0 on death day (but counts up from next day)

Streak logic:
- Current streak = days since last death (0 on death day, 1 next day, etc.)
- If never died, streak = days since first activity (or 1 if no history)
- Longest streak tracks max consecutive deathless days across all time
  </action>
  <verify>go build ./internal/graveyard/...</verify>
  <done>StreakInfo calculated from death records, current and longest streak tracked</done>
</task>

<task type="auto">
  <name>Task 2: Expose streak in IPC state response</name>
  <files>internal/ipc/protocol.go, internal/ipc/server.go, internal/daemon/daemon.go</files>
  <action>
Add streak to state response:

protocol.go:
- Extend StateResponse with: CurrentStreak (int), LongestStreak (int), TotalDeaths (int)

server.go / daemon.go:
- In get_state handler, include streak info from graveyard.GetStreakInfo()
- Include total deaths from graveyard.GetDeathCount()

This extends existing get_state response (used by TUI every 500ms) with streak data.
  </action>
  <verify>go build ./cmd/daemonflow/... && go build ./internal/...</verify>
  <done>StateResponse includes streak info, TUI receives streak on each refresh</done>
</task>

<task type="auto">
  <name>Task 3: Display streak in TUI</name>
  <files>tui/src/ipc.rs, tui/src/app.rs, tui/src/ui.rs</files>
  <action>
Add streak display to TUI:

ipc.rs:
- Extend StateResponse struct with: current_streak (i32), longest_streak (i32), total_deaths (i32)

app.rs:
- Add fields to App: current_streak (i32), longest_streak (i32), total_deaths (i32)
- Update refresh_state() to extract streak fields from response
- Default to 0 if fields missing (backwards compatibility with daemon without streak)

ui.rs:
- In render_status or render_stats section, add streak display:
  - "Streak: {current} days" (green if > 0, gray if 0)
  - "(Best: {longest})" small text beside
  - "Deaths: {total}" (red if > 0, hidden if 0)
- Place in stats section below clock display

Use subtle styling - streak is motivational, not primary focus.
  </action>
  <verify>cd tui && cargo build && cargo clippy</verify>
  <done>TUI displays current streak, longest streak, and total deaths</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go build ./... succeeds (Go side)
- [ ] cd tui && cargo build succeeds (Rust side)
- [ ] StreakInfo calculates correct streak from death records
- [ ] StateResponse includes streak and death count
- [ ] TUI displays streak info in status area
- [ ] Manual test: Run TUI, verify streak shows "0 days" initially, work productively for a day, verify streak increments
</verification>

<success_criteria>

- All tasks completed
- Streak calculated from death history
- IPC exposes streak in state response
- TUI displays streak with appropriate styling
- Longest streak tracked for motivation
- Total deaths shown as permanent record
- Phase 8 complete - Graveyard system fully functional
</success_criteria>

<output>
After completion, create `.planning/phases/08-graveyard/08-03-SUMMARY.md`
</output>
