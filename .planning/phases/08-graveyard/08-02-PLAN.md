---
phase: 08-graveyard
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified: [internal/graveyard/graveyard.go, internal/graveyard/record.go, internal/ipc/protocol.go, internal/ipc/server.go, internal/daemon/daemon.go, internal/clock/clock.go]
autonomous: true
---

<objective>
Implement pet recovery mechanics with cost system.

Purpose: Allow users to revive their pet after death, but at a cost (lose all earned time). This maintains stakes while not being punitive - you can always recover, but you start fresh.
Output: IPC resurrect command, recovery cost applied, recovery logged to GRAVEYARD.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior plan summary for graveyard foundation
@.planning/phases/08-graveyard/08-01-SUMMARY.md

# Graveyard package to extend
@internal/graveyard/graveyard.go
@internal/graveyard/record.go

# IPC protocol to add resurrect command
@internal/ipc/protocol.go
@internal/ipc/server.go

# Clock needs reset method for recovery
@internal/clock/clock.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resurrection state to graveyard</name>
  <files>internal/graveyard/record.go, internal/graveyard/graveyard.go</files>
  <action>
Extend graveyard package for recovery tracking:

record.go:
- Add ResurrectionRecord struct: Timestamp (time.Time), DeathIndex (int - which death was recovered from)
- Method ToMarkdownRow() returning "| {date} | {time} | Recovered from death #{n} |"

graveyard.go:
- Add field: Resurrections []ResurrectionRecord
- Add method LogResurrection() error - appends resurrection to GRAVEYARD.md
- Add method GetResurrectionCount() int
- Add method IsDead() bool - returns true if last event was death (deaths > resurrections)
- Update LoadRecords() to also parse resurrection entries

GRAVEYARD.md format extended:
```
# Graveyard

Your fallen pets rest here. Each death is a reminder to balance work and rest.

## Deaths

| Date | Time | Overtime | Cause |
|------|------|----------|-------|
| 2026-01-21 | 14:32 | 5m 23s | overtime |

## Resurrections

| Date | Time | Notes |
|------|------|-------|
| 2026-01-21 | 14:45 | Recovered from death #1 |
```
  </action>
  <verify>go build ./internal/graveyard/...</verify>
  <done>Graveyard tracks resurrections, IsDead() returns correct state</done>
</task>

<task type="auto">
  <name>Task 2: Add clock reset for recovery</name>
  <files>internal/clock/clock.go</files>
  <action>
Add reset capability to Clock:

1. Add method Reset():
   - Lock mutex
   - Set earnedSeconds = 0 (forfeit all earned time as recovery cost)
   - Set state = StateWorking
   - Set deathTriggered = false
   - Unlock

This is the "cost" of recovery - you lose all earned break time and start fresh.
  </action>
  <verify>go build ./internal/clock/...</verify>
  <done>Clock has Reset() method that zeroes earned time and returns to working state</done>
</task>

<task type="auto">
  <name>Task 3: Add resurrect IPC command</name>
  <files>internal/ipc/protocol.go, internal/ipc/server.go, internal/daemon/daemon.go</files>
  <action>
Add resurrect command to IPC:

protocol.go:
- Add constant: RequestTypeResurrect = "resurrect"
- Add ResurrectResponse struct: Success (bool), Message (string), NewState (string)

server.go:
- Add case for RequestTypeResurrect in handleRequest()
- Call daemon method d.Resurrect() and return ResurrectResponse

daemon.go:
- Add method Resurrect() (*ipc.ResurrectResponse, error):
  1. Check if graveyard.IsDead() - if not, return error "Pet is not dead"
  2. Call graveyard.LogResurrection()
  3. Call clock.Reset()
  4. Return success response with message "Pet resurrected! All earned time reset."

TUI will use this command to offer resurrection after detecting dead state.
  </action>
  <verify>go build ./cmd/daemonflow/... && go build ./internal/...</verify>
  <done>Resurrect IPC command works, resets clock, logs resurrection</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go build ./... succeeds
- [ ] Graveyard tracks both deaths and resurrections
- [ ] IsDead() correctly reflects death/resurrection balance
- [ ] Clock.Reset() zeroes earned time and returns to working
- [ ] Resurrect IPC command exists and daemon handles it
- [ ] Manual test: Kill pet (5 min overtime), call resurrect via daemonflow client, verify GRAVEYARD.md has resurrection entry
</verification>

<success_criteria>

- All tasks completed
- Recovery costs all earned time (reset to 0)
- Resurrection logged to GRAVEYARD.md
- IPC resurrect command functional
- Can only resurrect if pet is actually dead
- Ready for Plan 08-03 (streak tracking)
</success_criteria>

<output>
After completion, create `.planning/phases/08-graveyard/08-02-SUMMARY.md`
</output>
