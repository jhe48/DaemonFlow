---
phase: 12-global-task-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/config/config.go, internal/store/tasks.go, internal/store/schema.go]
autonomous: true
---

<objective>
Extend config to support multiple watch directories and add project tracking to task schema.

Purpose: Foundation for multi-directory monitoring - config and schema changes needed before tracker updates.
Output: Config supports `watch_dirs` array (backward-compatible with `watch_dir`), task schema includes priority scoring.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-sqlite-foundation/09-01-SUMMARY.md
@.planning/phases/09-sqlite-foundation/09-02-SUMMARY.md
@.planning/phases/12-global-task-sync/12-CONTEXT.md

@internal/config/config.go
@internal/store/tasks.go
@internal/store/schema.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend config for multi-directory support</name>
  <files>internal/config/config.go</files>
  <action>
Add `WatchDirs []string` field to Config struct with yaml tag `watch_dirs`. Keep existing `WatchDir string` for backward compatibility. In LoadConfig(), after parsing:
- If WatchDirs is empty but WatchDir is set, populate WatchDirs with single-element array containing WatchDir
- If both are set, WatchDirs takes precedence
- If neither is set, use cwd as default (current behavior)

Update DefaultConfig() to set WatchDirs as empty slice (will be populated from WatchDir in LoadConfig).

Add helper method `(c *Config) GetWatchDirs() []string` that returns WatchDirs (guaranteed non-empty after LoadConfig).
  </action>
  <verify>go build ./... succeeds, existing tests pass</verify>
  <done>Config supports both watch_dir (single string, backward compat) and watch_dirs (array), with GetWatchDirs() helper</done>
</task>

<task type="auto">
  <name>Task 2: Add priority scoring and project name to task schema</name>
  <files>internal/store/schema.go, internal/store/tasks.go</files>
  <action>
Add migration 2 in schema.go that adds:
- `project_name` TEXT column (extracted from project_path, e.g., "daemonflow" from "/home/user/daemonflow")
- `priority_score` INTEGER column DEFAULT 0 (computed score for ordering)

Update Task struct in tasks.go:
- Add `ProjectName string` field
- Add `PriorityScore int` field

In InsertTask(), extract project_name from project_path using filepath.Base() before insert.

Add new query function `GetTasksSortedByPriority() ([]Task, error)` that returns incomplete tasks ordered by:
1. priority_score DESC
2. due_date ASC (NULLs last)
3. created_at ASC

Note: priority_score will be computed/updated by a separate sync function in Plan 02.
  </action>
  <verify>go test ./internal/store/... passes, new migration applies cleanly</verify>
  <done>Task schema has project_name and priority_score columns, GetTasksSortedByPriority() returns priority-ordered incomplete tasks</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./...` succeeds
- [ ] `go test ./internal/config/... ./internal/store/...` passes
- [ ] Existing daemon starts without errors with old config format
- [ ] New config format with watch_dirs array parses correctly
</verification>

<success_criteria>

- All tasks completed
- Backward compatibility: existing single watch_dir configs still work
- Forward compatibility: watch_dirs array ready for multi-tracker use
- Priority scoring column ready for sync function
</success_criteria>

<output>
After completion, create `.planning/phases/12-global-task-sync/12-01-SUMMARY.md`
</output>
