---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [go.mod, go.sum, cmd/daemonflow/main.go, internal/daemon/daemon.go, internal/config/config.go, daemonflow.example.yaml]
autonomous: true
---

<objective>
Create the Go daemon skeleton with CLI, lifecycle management, and configuration loading.

Purpose: Establish the foundation that all other phases build upon — a daemon that can start, run persistently, and be configured.
Output: Working `daemonflow` binary that starts as a background daemon and loads configuration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and project structure</name>
  <files>go.mod, cmd/daemonflow/main.go</files>
  <action>
    1. Run `go mod init github.com/jackyhe0402/daemonflow`
    2. Create directory structure:
       - cmd/daemonflow/ (main entry point)
       - internal/daemon/ (daemon logic)
       - internal/config/ (configuration)
    3. Create minimal main.go with cobra CLI:
       - `daemonflow start` - start daemon
       - `daemonflow stop` - stop daemon (placeholder for now)
       - `daemonflow status` - check if running (placeholder)
    4. Use cobra for CLI (standard Go CLI library)
    5. Add version flag showing "0.1.0-dev"
  </action>
  <verify>go build ./cmd/daemonflow && ./cmd/daemonflow/daemonflow --version</verify>
  <done>Binary compiles and shows version</done>
</task>

<task type="auto">
  <name>Task 2: Implement daemon lifecycle management</name>
  <files>internal/daemon/daemon.go, cmd/daemonflow/main.go</files>
  <action>
    1. Create Daemon struct in internal/daemon/daemon.go:
       - Config field
       - Running bool
       - PID file path (~/.daemonflow/daemonflow.pid)
       - Methods: Start(), Stop(), Status() error
    2. Start() should:
       - Create ~/.daemonflow/ directory if not exists
       - Write PID to pid file
       - Set up signal handling (SIGINT, SIGTERM for graceful shutdown)
       - Run main loop (placeholder for now - just log "daemon running" every 30s)
    3. For actual daemonization, use simple approach:
       - When `start` is called, fork to background using `cmd.Start()` pattern
       - Parent exits, child continues
       - Use os/exec to re-run self with `--foreground` flag
    4. Stop() reads PID file and sends SIGTERM
    5. Status() checks if PID file exists and process is alive
  </action>
  <verify>
    ./cmd/daemonflow/daemonflow start
    sleep 2
    ./cmd/daemonflow/daemonflow status
    ./cmd/daemonflow/daemonflow stop
  </verify>
  <done>Daemon starts in background, status shows running, stop terminates it</done>
</task>

<task type="auto">
  <name>Task 3: Configuration loading from YAML</name>
  <files>internal/config/config.go, daemonflow.example.yaml</files>
  <action>
    1. Create Config struct in internal/config/config.go:
       - WatchDir string (directory to monitor, default: current dir)
       - LogLevel string (debug, info, warn, error - default: info)
       - SocketPath string (IPC socket location, default: ~/.daemonflow/daemonflow.sock)
       - Earning struct for Freedom Clock weights (placeholder for Phase 5)
    2. LoadConfig(path string) (*Config, error):
       - If path empty, look for ~/.daemonflow/config.yaml
       - If not found, use defaults
       - Parse YAML with gopkg.in/yaml.v3
    3. Create daemonflow.example.yaml with documented defaults
    4. Update Daemon.Start() to load config first
    5. Add --config flag to CLI to specify custom config path
  </action>
  <verify>
    cat daemonflow.example.yaml
    ./cmd/daemonflow/daemonflow start --config daemonflow.example.yaml
    ./cmd/daemonflow/daemonflow stop
  </verify>
  <done>Config loads from YAML, daemon uses configured values, example file documents all options</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go build ./cmd/daemonflow` succeeds
- [ ] `go vet ./...` reports no issues
- [ ] Daemon starts, runs in background, stops cleanly
- [ ] PID file created at ~/.daemonflow/daemonflow.pid
- [ ] Config file parsed correctly
- [ ] Signal handling works (Ctrl+C in foreground mode)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- `daemonflow start` runs daemon in background
- `daemonflow stop` terminates it cleanly
- `daemonflow status` reports running/stopped correctly
- Configuration loads from YAML file
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
