---
phase: 07-pet-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [tui/src/pet/art.rs, tui/src/pet/mod.rs]
autonomous: true
---

<objective>
Create ASCII art representations for each pet state.

Purpose: Each pet state needs distinct visual representation that users can immediately recognize - healthy pet looks vibrant, decaying pet looks sick, dead pet is clearly dead.
Output: Art module with ASCII art constants and getter function.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ASCII art for healthy and resting states</name>
  <files>tui/src/pet/art.rs, tui/src/pet/mod.rs</files>
  <action>
Create tui/src/pet/art.rs with ASCII art constants. Update mod.rs to declare art module.

Design a simple pet (cat, dog, or blob creature - keep it ~8-12 lines tall, ~20-30 chars wide for terminal compatibility).

Healthy state: Eyes open, upright posture, maybe a subtle happy expression.
```
const HEALTHY: &str = r#"
   /\_/\
  ( o.o )
   > ^ <
  /|   |\
 (_|   |_)
"#;
```

Resting state: Eyes closed, relaxed posture, maybe "zzz".
```
const RESTING: &str = r#"
   /\_/\   zzz
  ( -.- )
   > ~ <
  /|   |\
 (_|   |_)
"#;
```

Export a function to get art by state name (string for now, will use enum in plan 03).
  </action>
  <verify>cargo check passes</verify>
  <done>Healthy and Resting ASCII art defined and exported</done>
</task>

<task type="auto">
  <name>Task 2: Create ASCII art for tired, decaying, and dead states</name>
  <files>tui/src/pet/art.rs</files>
  <action>
Add remaining states to art.rs:

Tired state: Eyes droopy, slightly hunched, worried expression.
```
const TIRED: &str = r#"
   /\_/\
  ( o_o ) !
   > ~ <
  /|   |\
 (_|   |_)
"#;
```

Decaying state: Progressively worse - could have X eyes, slumped posture, sweat drops. Make it visually concerning.
```
const DECAYING: &str = r#"
   /\_/\
  ( x_x )
   > ~ < '
  /|   |\ '
 (_|___|_)
"#;
```

Dead state: Skull, gravestone, or clearly lifeless pet. This triggers emotional response.
```
const DEAD: &str = r#"
    ___
   /   \
  | RIP |
  |     |
  |_____|
   \___/
"#;
```

Create get_art(state: &str) -> &'static str function that returns appropriate art.
  </action>
  <verify>cargo check passes</verify>
  <done>All 5 pet states have ASCII art, get_art function returns correct art for each state</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes in tui/
- [ ] All 5 states have distinct, recognizable ASCII art
- [ ] Art is appropriately sized for terminal display (~10 lines, ~25 chars wide)
- [ ] get_art function exported and usable
</verification>

<success_criteria>

- 5 distinct ASCII art representations (Healthy, Resting, Tired, Decaying, Dead)
- Each state visually distinct and emotionally appropriate
- Art sized for typical terminal windows
- Function available to retrieve art by state
  </success_criteria>

<output>
After completion, create `.planning/phases/07-pet-system/07-02-SUMMARY.md`
</output>
