---
phase: 07-pet-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tui/src/pet/mod.rs, tui/src/pet/state.rs, tui/src/lib.rs]
autonomous: true
---

<objective>
Create pet state machine with states that map to productivity clock state.

Purpose: The pet's visual state should reflect the user's productivity - healthy when working, resting during breaks, decaying when in overtime.
Output: Pet module with PetState enum and transition logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tui-foundation/06-03-SUMMARY.md

@tui/src/app.rs
@tui/src/ipc/protocol.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pet module structure</name>
  <files>tui/src/pet/mod.rs, tui/src/lib.rs</files>
  <action>
Create new tui/src/pet/ directory with mod.rs. The mod.rs should declare submodules for state and (later) art. Update tui/src/lib.rs to export the pet module.

Module structure:
```
tui/src/pet/
├── mod.rs      (declares state submodule)
└── state.rs    (PetState enum and logic)
```
  </action>
  <verify>cargo check in tui/ directory passes</verify>
  <done>Pet module exists and compiles, lib.rs exports it</done>
</task>

<task type="auto">
  <name>Task 2: Implement PetState enum and transition logic</name>
  <files>tui/src/pet/state.rs</files>
  <action>
Create PetState enum with states that map to clock states:

```rust
pub enum PetState {
    Healthy,     // clock_state == "working" - actively earning time
    Resting,     // clock_state == "break" with positive time - enjoying earned break
    Tired,       // clock_state == "break" with low time (<60s) - break ending soon
    Decaying,    // clock_state == "overtime" - break exhausted, pet suffering
    Dead,        // overtime for extended period (e.g., overtime < -300s) - pet death
}
```

Implement transition function:
```rust
impl PetState {
    pub fn from_clock_state(clock_state: &str, earned_seconds: i32) -> Self {
        // Map clock state + earned balance to pet state
    }
}
```

Transition rules:
- "working" → Healthy (regardless of balance)
- "break" with earned_seconds >= 60 → Resting
- "break" with earned_seconds < 60 → Tired (warning)
- "overtime" with earned_seconds > -300 → Decaying
- "overtime" with earned_seconds <= -300 → Dead (5+ minutes overtime)

Dead threshold (-300s) can be a const for later configurability.
  </action>
  <verify>cargo test in tui/ passes (add unit tests for state transitions)</verify>
  <done>PetState::from_clock_state correctly maps all clock states to pet states with tests</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cargo check passes in tui/
- [ ] cargo test passes with state transition tests
- [ ] Pet module properly exported from lib.rs
</verification>

<success_criteria>

- PetState enum defined with all 5 states
- from_clock_state function correctly transitions based on clock state and balance
- Unit tests cover all transition paths
- Module structure ready for art and widget additions
  </success_criteria>

<output>
After completion, create `.planning/phases/07-pet-system/07-01-SUMMARY.md`
</output>
