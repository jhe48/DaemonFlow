---
phase: 13-pet-evolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [internal/store/pet.go, internal/store/pet_test.go, internal/daemon/daemon.go]
autonomous: true
---

<objective>
Add pet_state CRUD operations and XP/level calculation system to Go daemon.

Purpose: Enable persistence and calculation of pet experience and level progression based on productivity activities.
Output: Pet state store operations and XP award logic integrated with daemon activity events.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/store/store.go
@internal/store/schema.go
@internal/daemon/daemon.go
@internal/graveyard/graveyard.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pet_state CRUD operations to store package</name>
  <files>internal/store/pet.go, internal/store/pet_test.go</files>
  <action>
Create internal/store/pet.go with:
- PetState struct matching DB schema (ID, CurrentStreak, LongestStreak, TotalDeaths, TotalResurrections, Level, Experience, LastActivityDate, UpdatedAt)
- GetPetState(db) - read singleton row (id=1)
- UpdatePetState(db, state) - update singleton row
- AddExperience(db, amount) - atomic XP add with level-up check
- Level calculation: XP thresholds at 100, 300, 600, 1000, 1500 (levels 1-5, can extend)
- Helper: LevelFromExperience(xp) returns level and XP needed for next level

Create internal/store/pet_test.go with tests for:
- GetPetState returns default values
- UpdatePetState persists changes
- AddExperience increments correctly
- Level calculation thresholds
  </action>
  <verify>go test ./internal/store/... -run Pet -v passes</verify>
  <done>PetState CRUD operations work with level calculation</done>
</task>

<task type="auto">
  <name>Task 2: Integrate XP awards with daemon activity events</name>
  <files>internal/daemon/daemon.go</files>
  <action>
In daemon.go:
- Add store.Store reference to Daemon struct (already may exist for tasks)
- On activity events (commit, stage, file change, task complete), call store.AddExperience with appropriate amounts:
  - Commit: 10 XP
  - Stage: 2 XP
  - File save: 1 XP
  - Task complete: 5 XP
- Sync graveyard streak data to pet_state table on daemon start and after death/resurrection
- Update pet_state.TotalDeaths and TotalResurrections from graveyard counts

Do NOT change IPC protocol yet (that's plan 02).
  </action>
  <verify>go build ./... succeeds; daemon starts without errors</verify>
  <done>Daemon awards XP on productivity events and syncs streak data to SQLite</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go test ./internal/store/... -run Pet passes
- [ ] go build ./... succeeds
- [ ] Daemon starts and runs without errors
- [ ] Manual test: make a commit, check SQLite pet_state.experience increased
</verification>

<success_criteria>
- All tasks completed
- Pet state CRUD operations tested
- XP awarded on activity events
- Streak data synced from graveyard to SQLite
</success_criteria>

<output>
After completion, create `.planning/phases/13-pet-evolution/13-01-SUMMARY.md`
</output>
