---
phase: 13-pet-evolution
plan: 03
type: execute
wave: 2
depends_on: ["13-01", "13-02"]
files_modified: [tui/src/pet/art.rs, tui/src/pet/mod.rs]
autonomous: true
---

<objective>
Create level-based ASCII art progression for the pet, showing visual evolution as level increases.

Purpose: Provide rewarding visual feedback for leveling up - the pet visually grows/evolves.
Output: 5 levels of ASCII art with state variations, integrated into pet rendering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tui/src/pet/art.rs
@tui/src/pet/mod.rs
@tui/src/pet/state.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5-level ASCII art progression</name>
  <files>tui/src/pet/art.rs</files>
  <action>
Create level-based art constants. Each level has 5 state variants (Healthy, Resting, Tired, Decaying, Dead).

Progression concept (cat theme, growing bigger/more detailed):
- Level 1: Tiny kitten (~4 lines)
- Level 2: Small cat (~5 lines, current design)
- Level 3: Adult cat with more detail (~6 lines)
- Level 4: Majestic cat with accessories like collar (~6 lines)
- Level 5: Royal cat with crown/cape (~7 lines)

Structure as:
- LEVEL_1_HEALTHY, LEVEL_1_RESTING, LEVEL_1_TIRED, LEVEL_1_DECAYING, LEVEL_1_DEAD
- LEVEL_2_* (use current art as base)
- LEVEL_3_*, LEVEL_4_*, LEVEL_5_*

Keep dead state as gravestone across all levels (death is death).

Add function: get_art_for_level_and_state(level: i32, state: &PetState) -> &'static str
- Clamp level to 1-5 range
- Return appropriate art constant
  </action>
  <verify>cd tui && cargo build succeeds</verify>
  <done>25 ASCII art variants exist (5 levels x 5 states, with shared Dead)</done>
</task>

<task type="auto">
  <name>Task 2: Integrate level-aware art selection into Pet struct</name>
  <files>tui/src/pet/mod.rs</files>
  <action>
Update Pet struct and methods:
- Add level: i32 field to Pet struct
- Update Pet::new() to initialize level to 1
- Add Pet::set_level(level: i32) method
- Update Pet::get_art() to call get_art_for_level_and_state(self.level, &self.state)
- Export get_art_for_level_and_state from mod.rs if needed elsewhere

Keep backwards compatibility: existing get_art_for_state still works (can be used as fallback or for level 2).
  </action>
  <verify>cd tui && cargo build && cargo test succeeds</verify>
  <done>Pet struct uses level-aware art selection</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd tui && cargo build succeeds
- [ ] cd tui && cargo test succeeds
- [ ] All 5 levels have distinct healthy art
- [ ] Dead state shows gravestone regardless of level
</verification>

<success_criteria>
- All tasks completed
- 5 visual evolution stages exist
- Pet::get_art() returns level-appropriate art
- Art maintains emotional clarity at each level
</success_criteria>

<output>
After completion, create `.planning/phases/13-pet-evolution/13-03-SUMMARY.md`
</output>
