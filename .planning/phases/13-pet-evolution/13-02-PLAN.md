---
phase: 13-pet-evolution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [internal/ipc/protocol.go, internal/daemon/ipc_handler.go, tui/src/ipc/protocol.rs, tui/src/app.rs]
autonomous: true
---

<objective>
Extend IPC protocol to include level and experience data, update TUI to receive and store it.

Purpose: Enable the TUI to display pet level progression by receiving level/XP data from daemon.
Output: IPC protocol carries level/experience; TUI App state tracks these values.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@internal/ipc/protocol.go
@internal/daemon/ipc_handler.go
@tui/src/ipc/protocol.rs
@tui/src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add level/experience to Go IPC StateResponse</name>
  <files>internal/ipc/protocol.go, internal/daemon/ipc_handler.go</files>
  <action>
In internal/ipc/protocol.go:
- Add to StateResponse struct:
  - Level int `json:"level"` // Current pet level (1-5+)
  - Experience int `json:"experience"` // Current XP
  - ExperienceToNext int `json:"experience_to_next"` // XP needed for next level

In internal/daemon/ipc_handler.go (or wherever get_state is handled):
- When building StateResponse, read pet_state from store
- Populate Level, Experience, ExperienceToNext from store.GetPetState()
- Use store.LevelFromExperience() to get ExperienceToNext value
  </action>
  <verify>go build ./... succeeds</verify>
  <done>StateResponse includes level and experience fields populated from SQLite</done>
</task>

<task type="auto">
  <name>Task 2: Update Rust IPC protocol and App state</name>
  <files>tui/src/ipc/protocol.rs, tui/src/app.rs</files>
  <action>
In tui/src/ipc/protocol.rs:
- Add to StateResponse struct:
  - pub level: i32
  - pub experience: i32
  - pub experience_to_next: i32
- Add #[serde(default)] to new fields for backwards compatibility

In tui/src/app.rs:
- Add fields to App struct:
  - pub level: i32
  - pub experience: i32
  - pub experience_to_next: i32
- In App::new(), initialize to defaults (level=1, experience=0, experience_to_next=100)
- In update_state(), extract level/experience from StateResponse and store in App
  </action>
  <verify>cd tui && cargo build succeeds</verify>
  <done>TUI receives and stores level/experience data from daemon</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] go build ./... succeeds
- [ ] cd tui && cargo build succeeds
- [ ] cargo test in tui passes (if any protocol tests exist)
- [ ] Manual: run daemon, run TUI, verify no errors on state fetch
</verification>

<success_criteria>
- All tasks completed
- IPC protocol extended with level/experience
- TUI App receives and stores the new fields
- No breaking changes to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/13-pet-evolution/13-02-SUMMARY.md`
</output>
