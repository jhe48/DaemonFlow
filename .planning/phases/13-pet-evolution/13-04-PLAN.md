---
phase: 13-pet-evolution
plan: 04
type: execute
wave: 2
depends_on: ["13-02"]
files_modified: [tui/src/ui.rs, tui/src/app.rs]
autonomous: false
---

<objective>
Display pet level and XP progress in the TUI interface.

Purpose: Show users their progression towards the next level with clear visual feedback.
Output: Level indicator and XP progress bar visible in TUI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@tui/src/ui.rs
@tui/src/app.rs
@tui/src/pet/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add level/XP display to TUI layout</name>
  <files>tui/src/ui.rs, tui/src/app.rs</files>
  <action>
In tui/src/app.rs:
- In update_state(), call self.pet.set_level(state.level) to sync pet's visual level

In tui/src/ui.rs:
- Update render_pet() to include level in title: "Pet Lv.{level} ({state_name})"
- Add XP progress bar below or beside pet display:
  - Use ratatui Gauge widget or simple text representation
  - Show: "XP: {current}/{next} [{progress_bar}]"
  - Calculate percentage: (experience / experience_to_next) * 100
  - Progress bar: 10 chars, filled/unfilled (e.g., "████████░░")

Styling:
- Level text in Cyan or Gold color
- Progress bar fill in Green, empty in DarkGray
- Keep layout clean - don't make pet area too cluttered

Consider adding to the Clock status area instead if pet area gets crowded:
- Add "Lv.{level}" after streak info
- XP bar as separate line
  </action>
  <verify>cd tui && cargo build succeeds</verify>
  <done>Level and XP progress visible in TUI</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Level indicator and XP progress bar in TUI display</what-built>
  <how-to-verify>
    1. Start daemon: ./daemonflow start (or however it's run)
    2. Start TUI: cd tui && cargo run
    3. Verify level shows in pet title area (e.g., "Pet Lv.1 (Healthy)")
    4. Verify XP progress is visible (either as bar or text)
    5. Make some activity (save a file, complete a task) to earn XP
    6. Press 'r' to refresh and verify XP increased
    7. Check layout looks clean and not cluttered
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] cd tui && cargo build succeeds
- [ ] Level displays correctly in UI
- [ ] XP progress visible and updates
- [ ] Visual verification approved
</verification>

<success_criteria>
- All tasks completed
- Level and XP are visible in TUI
- Layout is clean and readable
- User approved visual design
</success_criteria>

<output>
After completion, create `.planning/phases/13-pet-evolution/13-04-SUMMARY.md`
</output>
