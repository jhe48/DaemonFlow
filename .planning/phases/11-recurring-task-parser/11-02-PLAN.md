---
phase: 11-recurring-task-parser
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified: [brain/recurring/unroller.py, brain/main.py, internal/task/recurring.go, internal/task/recurring_test.go]
autonomous: true
---

<objective>
Add RRULE unrolling to Python brain and Go integration for recurring task detection.

Purpose: Enable horizon-based unrolling of RRULEs to concrete dates, and wire Go task system to call Python brain for recurrence detection.
Output: Complete recurring task pipeline from detection through unrolling, callable from Go.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-recurring-task-parser/11-RESEARCH.md
@.planning/phases/11-recurring-task-parser/11-01-SUMMARY.md

@brain/main.py
@brain/recurring/parser.py
@internal/brain/executor.go
@internal/task/parser.go
@internal/store/tasks.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RRULE unroller module</name>
  <files>brain/recurring/unroller.py</files>
  <action>
Create the unroller module that expands RRULEs to concrete dates:

1. Create `brain/recurring/unroller.py` with:

   - `unroll_occurrences(rrule_str: str, start: datetime = None, horizon_days: int = 7) -> list[str]`:
     - Uses dateutil.rrule.rrulestr to parse RRULE
     - Gets occurrences between start and start+horizon using rule.between()
     - Returns list of ISO 8601 date strings (YYYY-MM-DD)
     - Default horizon: 7 days (configurable)
     - Handle invalid RRULE gracefully (return empty list)

   - `task_instance_id(task_text: str, rrule: str, occurrence_date: str) -> str`:
     - Generate stable deterministic ID for deduplication
     - Hash (task_text, rrule, date) with SHA256, take first 16 chars
     - This prevents duplicate task creation on re-parse

2. Handle edge cases:
   - Empty/invalid RRULE → return []
   - RRULE with no occurrences in horizon → return []
   - Don't crash on dateutil errors

Note: Use datetime from stdlib, not dateparser. dateutil handles RRULE expansion.
  </action>
  <verify>python -c "from brain.recurring.unroller import unroll_occurrences; print(unroll_occurrences('RRULE:FREQ=DAILY', horizon_days=3))"</verify>
  <done>unroll_occurrences returns list of ISO date strings for next N days</done>
</task>

<task type="auto">
  <name>Task 2: Add unroll action to brain CLI</name>
  <files>brain/main.py</files>
  <action>
Add unroll_recurring action to the brain CLI:

1. Add import: `from brain.recurring.unroller import unroll_occurrences, task_instance_id`

2. Create `unroll_recurring_action(data: dict) -> dict`:
   - Expects:
     - data["rrule"]: RRULE string to unroll
     - data["task_text"]: Original task text (for ID generation)
     - data["horizon_days"]: Optional, default 7
   - Returns:
     ```python
     {
         "occurrences": [
             {"date": "2026-01-28", "instance_id": "abc123..."},
             {"date": "2026-01-29", "instance_id": "def456..."},
             ...
         ]
     }
     ```
   - Each occurrence has a stable instance_id for deduplication

3. Add to ACTIONS dict: `"unroll_recurring": unroll_recurring_action`

4. Update argparse choices and docstring
  </action>
  <verify>python -m brain --action unroll_recurring --input '{"rrule": "RRULE:FREQ=DAILY", "task_text": "test", "horizon_days": 3}' | python -c "import sys,json; d=json.load(sys.stdin); assert len(d['occurrences'])>=2; print('OK')"</verify>
  <done>unroll_recurring action returns list of occurrences with dates and stable instance IDs</done>
</task>

<task type="auto">
  <name>Task 3: Create Go recurring task helper</name>
  <files>internal/task/recurring.go, internal/task/recurring_test.go</files>
  <action>
Create Go helper to call Python brain for recurring task detection:

1. Create `internal/task/recurring.go`:

   - `ParseRecurringResponse` struct:
     ```go
     type ParseRecurringResponse struct {
         Recurring    bool   `json:"recurring"`
         RRule        string `json:"rrule,omitempty"`
         CleanText    string `json:"clean_text,omitempty"`
         OriginalText string `json:"original_text,omitempty"`
     }
     ```

   - `UnrollResponse` struct:
     ```go
     type UnrollResponse struct {
         Occurrences []struct {
             Date       string `json:"date"`
             InstanceID string `json:"instance_id"`
         } `json:"occurrences"`
     }
     ```

   - `ParseRecurring(ctx context.Context, executor *brain.Executor, text string) (*ParseRecurringResponse, error)`:
     - Calls executor.Execute(ctx, "parse_recurring", {"text": text})
     - Unmarshals response into ParseRecurringResponse
     - Returns nil, nil if executor is nil (graceful when brain unavailable)

   - `UnrollRecurring(ctx context.Context, executor *brain.Executor, rrule, taskText string, horizonDays int) (*UnrollResponse, error)`:
     - Calls executor.Execute(ctx, "unroll_recurring", {"rrule": rrule, "task_text": taskText, "horizon_days": horizonDays})
     - Unmarshals response
     - Returns nil, nil if executor is nil

2. Create `internal/task/recurring_test.go` with tests:
   - TestParseRecurring_NilExecutor (returns nil, nil)
   - TestUnrollRecurring_NilExecutor (returns nil, nil)
   - Skip integration tests if Python unavailable (use build tag or check)

Note: These are helper functions that wrap the executor. Actual task tracker integration happens in Phase 12 (Global Task Sync).
  </action>
  <verify>go test ./internal/task/... -run Recurring -v</verify>
  <done>Go helper functions compile, nil executor tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m brain --action unroll_recurring --input '{"rrule": "RRULE:FREQ=WEEKLY;BYDAY=FR", "task_text": "Review PRs", "horizon_days": 14}'` returns occurrences
- [ ] Instance IDs are deterministic (same input → same ID)
- [ ] `go build ./...` succeeds
- [ ] `go test ./internal/task/...` passes
- [ ] Full pipeline test: parse_recurring → unroll_recurring works
</verification>

<success_criteria>
- All tasks completed
- Python unroller creates dated occurrences from RRULEs
- Stable instance IDs generated for deduplication
- Go helper functions created for executor integration
- Nil executor handled gracefully (no crash)
- Ready for Phase 12 integration into task tracker
</success_criteria>

<output>
After completion, create `.planning/phases/11-recurring-task-parser/11-02-SUMMARY.md`
</output>
