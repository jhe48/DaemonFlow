---
phase: 11-recurring-task-parser
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [brain/recurring/__init__.py, brain/recurring/parser.py, brain/recurring/patterns.py, brain/main.py, requirements.txt]
autonomous: true
---

<objective>
Build Python recurring task parser with natural language detection and RRULE conversion.

Purpose: Enable natural language recurrence parsing ("every friday", "daily at 9am") using recurrent library, outputting RFC 5545 RRULEs for dateutil expansion.
Output: Python recurring module with parse_recurring action callable via Go executor.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-recurring-task-parser/11-RESEARCH.md
@.planning/phases/11-recurring-task-parser/11-CONTEXT.md
@.planning/phases/10-python-brain-setup/10-01-SUMMARY.md

@brain/main.py
@brain/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recurring patterns module</name>
  <files>brain/recurring/__init__.py, brain/recurring/patterns.py</files>
  <action>
Create the recurring subpackage with pattern detection:

1. Create `brain/recurring/__init__.py` as package marker
2. Create `brain/recurring/patterns.py` with:
   - RECURRENCE_PATTERNS: List of regex patterns for detection
     - `\bevery\s+\w+` (every day, every monday)
     - `\bdaily\b` (daily)
     - `\bweekly\b` (weekly)
     - `\bmonthly\b` (monthly)
     - `\b\w+days?\b` (fridays, mondays)
     - `\bevery\s+other\b` (every other)
   - `has_recurrence(text: str) -> bool`: Returns True if text contains any pattern
   - `extract_task_text(text: str) -> str`: Removes recurrence words to get clean task

Key: Only detect explicit recurring patterns. Avoid false positives on words like "everyday" used as adjective.
  </action>
  <verify>python -c "from brain.recurring.patterns import has_recurrence; print(has_recurrence('Review PRs every friday'))"</verify>
  <done>has_recurrence returns True for recurring patterns, False for regular tasks</done>
</task>

<task type="auto">
  <name>Task 2: Create RRULE parser with recurrent wrapper</name>
  <files>brain/recurring/parser.py, requirements.txt</files>
  <action>
Create the parser module that converts natural language to RRULEs:

1. Update requirements.txt to add:
   - recurrent==0.4.1
   - python-dateutil>=2.8.0

2. Create `brain/recurring/parser.py` with:
   - `parse_recurrence(text: str, now: datetime = None) -> dict | None`:
     - Use patterns.has_recurrence() for early detection
     - If not recurring, return None
     - Use RecurringEvent from recurrent.event_parser
     - Call r.parse(text), get r.get_RFC_rrule()
     - Return dict with:
       - `rrule`: The RFC 5545 RRULE string (e.g., "RRULE:FREQ=WEEKLY;BYDAY=FR")
       - `clean_text`: Task text with recurrence words removed
       - `original_text`: Original input text
     - Handle recurrent failures gracefully (return None, don't crash)

3. Add wrapper with error handling for recurrent's quirks:
   - Catch exceptions from recurrent library
   - Return None if parsing fails (graceful degradation)

Note: recurrent is unmaintained (2021) but functional. Wrap all recurrent calls in try/except.
  </action>
  <verify>python -c "from brain.recurring.parser import parse_recurrence; print(parse_recurrence('Review PRs every friday'))"</verify>
  <done>parse_recurrence returns {"rrule": "RRULE:FREQ=WEEKLY;BYDAY=FR", "clean_text": "Review PRs", "original_text": "..."} for recurring tasks</done>
</task>

<task type="auto">
  <name>Task 3: Add parse_recurring action to brain CLI</name>
  <files>brain/main.py</files>
  <action>
Register the parse_recurring action in the brain CLI:

1. Add import: `from brain.recurring.parser import parse_recurrence`

2. Create `parse_recurring_action(data: dict) -> dict`:
   - Expects data["text"] with task text to parse
   - Calls parse_recurrence(data["text"])
   - Returns result dict or {"recurring": False} if not recurring
   - Format response:
     ```python
     if result:
         return {
             "recurring": True,
             "rrule": result["rrule"],
             "clean_text": result["clean_text"],
             "original_text": result["original_text"]
         }
     else:
         return {"recurring": False}
     ```

3. Add to ACTIONS dict: `"parse_recurring": parse_recurring_action`

4. Update argparse choices to include "parse_recurring"

5. Update docstring to document new action
  </action>
  <verify>python -m brain --action parse_recurring --input '{"text": "Review PRs every friday"}' | python -c "import sys,json; d=json.load(sys.stdin); assert d['recurring']==True; print('OK')"</verify>
  <done>parse_recurring action callable via CLI, returns recurring:true/false with RRULE when applicable</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pip install -r requirements.txt` succeeds (recurrent, dateutil installed)
- [ ] `python -m brain --action parse_recurring --input '{"text": "daily standup"}'` returns recurring:true
- [ ] `python -m brain --action parse_recurring --input '{"text": "Buy groceries"}'` returns recurring:false
- [ ] `python -m brain --action ping --input '{}'` still works (no regressions)
</verification>

<success_criteria>
- All tasks completed
- Python recurring module created with patterns + parser
- parse_recurring action registered in brain CLI
- Natural language patterns detected ("every X", "daily", "weekly", "monthly")
- RRULE strings generated for valid recurring patterns
- Graceful handling when recurrent library fails
</success_criteria>

<output>
After completion, create `.planning/phases/11-recurring-task-parser/11-01-SUMMARY.md`
</output>
